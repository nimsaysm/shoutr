<nav class="navbar navbar-expand-lg navbar-dark bg-dark d-flex justify-content-between">
    <div class="container-fluid">
            <%= link_to "Home", root_path, class: "navbar-brand" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse p-2" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto d-flex align-items-center">
                <% if signed_in? %>
                    <li class="nav-item">
                        <p class="d-flex flex-nowrap nav-link mb-0 align-items-center">
                            <%= link_to "Signed in as: @" + current_user.username, current_user, class: "nav-link" %>
                        </p>
                    </li>
                    <li class="nav-item">
                        <%= button_to 'Sign out', sign_out_path, method: :delete, class: "nav-link" %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to 'Sign in', sign_in_path, class: "nav-link" %>
                    </li>
                <% end %>
            </ul>

            <%= render "search" %>
        </div>
</nav>